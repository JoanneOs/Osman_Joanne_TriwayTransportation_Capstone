import React from 'react';
import { Typography } from '@mui/material';

const Shipments = () => {
  return (
    <div>
      <Typography variant="h4">Shipments Management</Typography>
      <Typography paragraph>This is where you'll manage shipments.</Typography>
    </div>
  );
};

export default Shipments;